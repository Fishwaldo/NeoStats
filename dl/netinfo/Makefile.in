INCLUDES = -I../..

SOURCES= netinfo.c
OBJECTS= netinfo.lo
TARGET= netinfo.so

CC=@CC@
CFLAGS=@CFLAGS@ -fPIC
LINKER=ld

all:
	$(CC) $(CFLAGS) $(INCLUDES) -o $(OBJECTS) -c $(SOURCES)
	$(LINKER) -shared -o $(TARGET) $(LIBS) $(OBJECTS)
	/bin/cp $(TARGET) ../

clean:
	/bin/rm -rf ../$(TARGET) Makefile
	/bin/rm -rf *.o *.lo *.so